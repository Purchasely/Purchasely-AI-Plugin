---
description: "Migrate Purchasely SDK from v5.x to v6.0 — scan for breaking changes and apply fixes"
argument-hint: "[platform: ios|android]"
---

Invoke the `purchasely:migrate` skill with these arguments: $ARGUMENTS
