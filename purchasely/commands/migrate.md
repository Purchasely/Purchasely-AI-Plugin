---
description: "Migrate an existing Purchasely SDK integration to a newer SDK major version"
argument-hint: "[platform: android|ios] [from:5.x] [to:6.0.0-rc.1]"
---

# Purchasely SDK Migration

The user invoked this command with: $ARGUMENTS

Read `skills/purchasely-migrate/SKILL.md` from this plugin, then follow that skill exactly. Pass the arguments above as the skill's `$ARGUMENTS`.
