---
description: "Debug Purchasely SDK issues — diagnose blank paywalls, frozen UI, purchase failures, and more"
argument-hint: "<describe the issue>"
---

# Purchasely SDK Debugging

The user invoked this command with: $ARGUMENTS

Read `skills/purchasely-debug/SKILL.md` from this plugin, then follow that skill exactly. Pass the arguments above as the skill's `$ARGUMENTS`.
